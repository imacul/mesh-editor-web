Optional: place a sample mesh here as:
  /public/models/sample.glb

You can also upload local STL/OBJ/PLY/GLB/GLTF files directly from the app.
If model loading fails, the app automatically loads a mock box mesh so the editor still works.
